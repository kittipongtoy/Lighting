@{
    ViewData["Title"] = "Manage_Project_Page";
    Layout = "~/Views/Shared/Layout/_Layout.cshtml";
}
@model IEnumerable<Lighting.Areas.Identity.Data.Category_Project>

<div class="container" style="width:100%;">

    <div class="row d-flex justify-content-between mb-3">
        <div class="col d-flex justify-content-between">
            <div>
                <button class="btn btn-primary" id="btn_add_category">เพิ่มหมวดหมู่/add category</button>
            </div>
            <a href="@Url.Action("Manage_Page","ProjectRef")" class="btn btn-primary rounded-2">
                กลับ/back
            </a>
        </div>
    </div>

    <div class="row mt-3" style="display:none;" id="add_category">
        <div class="col-12" style="display:flex;justify-content:center;flex-direction:column;align-items:center;">
            <img style="width:45%;display:none;" id="img_preview" />
            <img style="width:45%;display:none;" id="img_preview_nav" />
        </div>
        <div class="col-12 " style="margin-top:3rem;">
            <div class="input-group">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="">ชื่อหมวดหมู่/Category Name</span>
                </div>
                <input type="text" class="form-control" id="category_name_th" placeholder="ภาษาไทย">
                <input type="text" class="form-control" id="category_name_en" placeholder="English">
            </div>
            <label class="form-label mt-3">เลือกรูปหมวดหมู่/image category                
                    <br />
                    <span style="color:red;">
                        ขนาดรูปภาพ : 1280 * 960 px
                    </span>
            </label>
            <input class="form-control mt-3" type="file" id="file_img_preview">
            <label class="form-label mt-3">เลือกรูปแนบบา/image category navbar
                <br />
                <span style="color:red;">
                    ขนาดรูปภาพ : 800 * 800 px
                </span>
            </label>
            <input class="form-control mt-3" type="file" id="file_img_preview_nav" />
        </div>
        <center><button class="btn btn-success" style="margin-top:3rem;" onclick="addCategory()">บันทึก/save</button></center>
    </div>



    <div class="row mt-5">
        <div class="col-12">
            <table class="table mt-3">
            <thead>
                <tr style="border-top:1px solid black;border-bottom:1px solid black;">
                    <th scope="col">#</th>
                    <th scope="col">รูปปก/image preview</th>
                    <th scope="col">หัวข้อ/Title</th>
                    <th scope="col" class="text-center">จัดการ/Manage</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int i = 1;
                }
                @foreach (var project in Model)
                {
                    <tr id="@project.Id">
                        <td scope="row">@i</td>
                        <td><img src="@project.Image_Path" style="width:100px;height:100px;" /> </td>
                            <td style="word-wrap:break-word;max-width:500px;">@project.Name_TH / @project.Name_EN</td>
                        <td class="text-center">
                                <button type="button" class="btn btn-warning" onclick="edit(@project.Id,'@project.Image_Path?.Replace('\\','/')','@project.Image_Path_Nav?.Replace('\\','/')','@project.Name_TH','@project.Name_EN')">
                                <i class="bi bi-pencil-square"></i>แก้ไข/edit
                            </button>
                            <button type="button" class="btn btn-danger" onclick="deleteRowById(@project.Id)">
                                <i class="bi bi-trash-fill"></i>ลบ/delete
                            </button>
                        </td>
                        @{
                            i++;
                        }
                    </tr>
                }
            </tbody>
        </table>
        </div>
    </div>
</div>

<div style="position:fixed;inset:0;background-color:rgba(0,0,0,0.5);display:none;justify-content:center;flex-direction:column;align-items:center;z-index:10;overflow:auto;" id="edit_page">
    <div style="width:50%;min-width:428px;height:auto;background-color:white;padding:3rem;border-radius:10px;">
        <div style="display:grid;grid-template-rows:auto 4rem 4rem 4rem 3rem;gap:3rem;">
            <div style="display:grid;grid-template-columns:50% 50%;">
                <img style="width:100%;height:50vh;" id="edit_img_preview" />
                <img style="width:100%;height:50vh;" id="edit_img_preview_nav" />
            </div>

            <div>
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text" id="">ชื่อหมวดหมู่/Category Name</span>
                    </div>
                    <input type="text" class="form-control" id="edit_category_name_th" placeholder="ภาษาไทย">
                    <input type="text" class="form-control" id="edit_category_name_en" placeholder="English">
                </div>

            </div>

            <div>

                <div style="display:grid;grid-template-columns:auto auto;">
                    <label class="form-label mt-3">
                        เลือกรูปหมวดหมู่/image category                 
                        <span style="color:red;">
                            ขนาดรูปภาพ : 1280 * 960 px
                        </span>
                    </label>
                    <input class="form-control mt-3" type="file" id="edit_file_img_preview">
                </div>
            </div>
            <div style="display:grid;grid-template-columns:auto auto;">

                <label class="form-label mt-3">เลือกรูปแนบบา/image category navbar
                    <span style="color:red;">
                        ขนาดรูปภาพ : 800 * 800 px
                    </span>
                </label>
                <input class="form-control mt-3" type="file" id="edit_file_img_preview_nav">
            </div>
            <center>
                <button class="btn btn-success" onclick="saveEdit()">บันทึก/save</button>
                <button class="btn btn-primary btn-danger"  onclick="closeEdit()">ปิด/close</button>
            </center>

        </div>
    </div>
</div>

@section Scripts{
    <script>

        const blobToBase64DataURL = blob => new Promise(
            resolvePromise => {
                const reader = new FileReader();
                reader.onload = () => resolvePromise(reader.result);
                reader.readAsDataURL(blob);
            }
        );

        var edit_id;
        var add_img_file;
        var edit_img_file, edit_img_file_nav;
        var img_preview_nav;

        $(document).ready(function () {

            $('#file_img_preview').change(async function (e) {
                if (e.target.files.length === 1) {
                    add_img_file = e.target.files[0];
                    $('#img_preview').attr('src', await blobToBase64DataURL(add_img_file)).css("display", "block");
                }
            });

            $('#file_img_preview_nav').change(async function (e) {
                if (e.target.files.length === 1) {
                    img_preview_nav = e.target.files[0];
                    $('#img_preview_nav').attr('src', await blobToBase64DataURL(img_preview_nav)).css("display", "block");
                }
            });

            $('#edit_file_img_preview').change(async function (e) {
                if (e.target.files.length === 1) {
                    edit_img_file = e.target.files[0];
                    $('#edit_img_preview').attr('src', await blobToBase64DataURL(edit_img_file));
                }
            });

            $('#edit_file_img_preview_nav').change(async function (e) {
                if (e.target.files.length === 1) {
                    edit_img_file_nav = e.target.files[0];
                    $('#edit_img_preview_nav').attr('src', await blobToBase64DataURL(edit_img_file_nav));
                }
            });

            $("#btn_add_category").on("click", function () {
                let display = $("#add_category").css("display");
                if (display == "none") {
                    $("#add_category").css("display", "block");
                } else {
                    $("#add_category").css("display", "none");
                }
            });

        });

        function addCategory() {
            let category_name_th = $("#category_name_th").val();
            let category_name_en = $("#category_name_en").val();
            let form = new FormData();
            form.append("Name_TH", category_name_th);
            form.append("Name_EN", category_name_en);
            if (add_img_file != undefined) {
                form.append("Image_File", add_img_file);
            }

            if (img_preview_nav != undefined) {
                form.append("Image_File_Nav", img_preview_nav);
            }

            //ajax
            $.ajax({
                type: 'POST',
                url: '/ManageProjectCategory/add',
                data: form,
                processData: false,
                contentType: false,
                cache: false,
                beforeSend: function () {
                    Swal.fire('Please wait')
                    Swal.showLoading()
                },
                success: function (data) {
                    if (data.status == "success") {
                        Swal.fire({
                            icon: 'success',
                            title: data.message,
                            showConfirmButton: false,
                            timer: 2000
                        }).then(() => {
                            window.location.href = "/ManageProjectCategory/Manage_Page";
                        });
                    }
                    else {
                        Swal.fire({
                            icon: 'warning',
                            text: data.message,
                        });
                    }
                }, error: function (e) {
                    Swal.fire({
                        icon: 'error',
                        text: e.message,
                    });
                    console.log(e.message);
                    console.log(e.inner);
                }
            });
        }

        function closeEdit(){
            $("#edit_page").css("display", "none");
        }

        function saveEdit(){

            if(!edit_id) {
                Swal.fire({
                    icon: 'error',
                    text: "กรุณาเลือกค้าที่ต้องการแก้ไข",
                });
            }

            let category_name_th = $("#edit_category_name_th").val();
            let category_name_en = $("#edit_category_name_en").val();


            let form = new FormData();
            form.append("Name_TH", category_name_th);
            form.append("Name_EN", category_name_en);

            if (edit_img_file != undefined) {
                form.append("Image_File", edit_img_file);
            }

            if (edit_img_file_nav != undefined) {
                form.append("Image_File_Nav", edit_img_file_nav);
            }
            //ajax
            $.ajax({
                type: 'POST',
                url: '/ManageProjectCategory/edit?id=' + edit_id,
                data: form,
                processData: false,
                contentType: false,
                cache: false,
                beforeSend: function () {
                    Swal.fire('Please wait')
                    Swal.showLoading()
                },
                success: function (data) {
                    if (data.status == "success") {
                        Swal.fire({
                            icon: 'success',
                            title: data.message,
                            showConfirmButton: false,
                            timer: 2000
                        }).then(() => {
                            //append file
                            edit_id = null;
                            window.location.href = "/ManageProjectCategory/Manage_Page";
                        });
                    }
                    else {
                        Swal.fire({
                            icon: 'warning',
                            text: data.message,
                        });
                    }
                }, error: function (e) {
                    Swal.fire({
                        icon: 'error',
                        text: e.message,
                    });
                    console.log(e.message);
                    console.log(e.inner);
                }
            });
        }

        function edit(id, img_path,img_nav, name_th, name_en) {
            edit_id = id;
            $("#edit_page").css("display","flex");
            $("#edit_category_name_th").val(name_th);
            $("#edit_category_name_en").val(name_en);
            $('#edit_img_preview').attr('src', img_path);
            $('#edit_img_preview_nav').attr('src', img_nav);
        }

        function deleteRowById(id) {

            Swal.fire({
                icon: 'warning',
                title: "คุณแน่ใจว่าต้องการลบ?",
                text: "",
                showConfirmButton: true,
            }).then((confirm) => {
                if (confirm.isConfirmed) {

                    $.ajax({
                        type: 'POST',
                        url: '/ManageProjectCategory/DeleteById?id=' + id,
                        processData: false,
                        contentType: false,
                        cache: false,
                        beforeSend: function () {
                            Swal.fire('Please wait')
                            Swal.showLoading()
                        },
                        success: function (data) {
                            if (data.status == "success") {
                                Swal.fire({
                                    icon: 'success',
                                    title: data.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                }).then(() => {

                                    $("#" + id).remove();
                                });
                            }
                            else {
                                Swal.fire({
                                    icon: 'warning',
                                    text: data.message,
                                });
                            }
                        }, error: function (e) {
                            Swal.fire({
                                icon: 'error',
                                text: e.message,
                            });
                            console.log(e.message);
                            console.log(e.inner);
                        }
                    });

                } else {

                }
            });
        }

    </script>
}
